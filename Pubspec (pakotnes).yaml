dependencies:
  flutter:
    sdk: flutter

  # State
  flutter_riverpod: ^2.5.1

  # Firebase
  firebase_core: ^3.4.0
  firebase_auth: ^5.2.0
  cloud_firestore: ^5.4.0

  # Location + permissions + compass
  geolocator: ^12.0.0
  permission_handler: ^11.3.1
  flutter_compass: ^0.8.0

  # Maps
  google_maps_flutter: ^2.6.1

  # QR
  mobile_scanner: ^5.2.3

  # Utilities
  collection: ^1.18.0
